<% if @upload.errors.any? %>
	<div id="error_explanation">
		<div class="alert alert-danger">
			There are <%= pluralize(@upload.errors.count, "error") %>.
		</div>
		<ul>
			<% @upload.errors.full_messages.each do |msg| %>
				<li><%= msg %></li>
			<% end %>
		</ul>
	</div>
<% end %>
